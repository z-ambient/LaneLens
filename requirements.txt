# Pinned to exact versions so local, CI, and the Railway Docker build all
# install the same thing, and nothing new enters production without a
# reviewed bump (Dependabot opens the PRs; pip-audit gates CI on CVEs).
fastapi==0.139.0
uvicorn==0.51.0
requests==2.34.2
python-dotenv==1.2.2
openai==2.44.0
pytest==9.1.1
httpx==0.28.1
sqlalchemy==2.0.51
psycopg2-binary==2.9.12
slowapi==0.1.10
